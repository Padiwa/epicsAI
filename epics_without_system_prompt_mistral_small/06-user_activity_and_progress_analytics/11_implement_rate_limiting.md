# Implement Rate Limiting
- Use github.com/ulule/limiter for API rate limiting.
- Apply different limits for authenticated vs. unauthenticated users.
- Store rate limit counters in Redis or similar.
```

```markdown
