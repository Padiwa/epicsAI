# Design Database Schema
- Define schema for storing user activity data (exercises, sets, reps, weights, timestamps).
- Use GORM for ORM to interact with the database (PostgreSQL recommended).
- Include tables for users, workouts, exercises, and progress metrics.
```

```markdown
